# 公网服务器

应用默认只监听 `127.0.0.1`。公网服务器应使用独立域名、TLS 反向代理和主机防火墙，只开放 80/443。不要直接暴露应用端口，也不要在 URL、日志或命令历史中传管理员令牌。

先完成本机健康检查，再将反向代理指向 `127.0.0.1:8000`。GitHub App 的 Webhook URL 为 `https://你的域名/webhooks/github`。

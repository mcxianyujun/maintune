# 仓库配置

每个仓库单独配置 Installation ID、默认分支、工作目录、安装/测试/lint/build 命令，以及 Issue、PR review、auto merge 开关。

Auto merge 仅适用于 Maintainer 自己从 Issue workflow 创建的 PR，并要求独立审核、测试、CI/checks、仓库策略和 Owner decision 全部满足。外部贡献者 PR 默认只审核，不接管分支。
